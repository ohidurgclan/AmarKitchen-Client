const AdminSettings = () => {
  return (
    <div>
      <h2>Settings</h2>
      <p>Manage admin panel settings here.</p>
    </div>
  );
};

export default AdminSettings;  // Ensure it's default export
